# Kasa Sistemi Update

This repository hosts `update.json` used by the desktop app to enforce mandatory updates.

- `update.json` is fetched from GitHub raw.
- `downloadUrl` should point to the latest release.
